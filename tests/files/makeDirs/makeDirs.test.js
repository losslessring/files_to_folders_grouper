import { describe, it, expect } from '../../../testingLibrary/testingLibrary.js'

import path from 'path'
import { makeDirs } from './../../../src/files/makeDirs'
import { getDirs } from './../../../src/files/getDirs'

export const makeDirs_test = () => {
    describe('make directories', () => {
        // it('make directories', () => {
        //     const resolvedPath =
        //         path.resolve() + '\\' + 'tests\\files\\makeDirs\\testFolder0'
        //     const dirNames = [
        //         resolvedPath + '\\testDir0',
        //         resolvedPath + '\\testDir1',
        //     ]
        //     makeDirs(dirNames)
        //     const createdDirName =
        //         path.resolve() + '\\' + 'tests\\files\\makeDirs\\testFolder0\\'
        //     const result = getDirs(createdDirName)
        //     const expected = ['testDir0', 'testDir1']
        //     expect(result).toBe(expected)
        // })
    })
}
